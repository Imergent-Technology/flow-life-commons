# Local environment and secrets. Only *.example files are committed.
.env
.env.*
!.env.example
# The production TEMPLATE, which is documentation and holds no secret: every value that is secret is
# left blank for the operator to supply on the host. The real production file never leaves the host.
!.env.production.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Dependencies and build output
node_modules/
vendor/
dist/

# Test / tooling output
apps/guardian-console/e2e/.fixtures/
coverage/
test-results/
playwright-report/
.phpunit.result.cache
.phpunit.cache/
.php-cs-fixer.cache
.eslintcache
*.log
