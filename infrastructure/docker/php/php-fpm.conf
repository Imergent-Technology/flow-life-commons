; The container runs as the developer's UID/GID rather than root, so FPM cannot
; drop privileges; the `user`/`group` directives from the stock pool are ignored.
[www]
clear_env = no
catch_workers_output = yes
decorate_workers_output = no
